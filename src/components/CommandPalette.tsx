import {useEffect,useState} from 'react';import {Search} from 'lucide-react';import {useNavigate} from 'react-router-dom';import {Input,Modal} from './ui';
export function CommandPalette(){const [open,setOpen]=useState(false);const nav=useNavigate();useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key==='k'){e.preventDefault();setOpen(x=>!x)}};addEventListener('keydown',fn);return()=>removeEventListener('keydown',fn)},[]);const go=(p:string)=>{nav(p);setOpen(false)};return <Modal open={open} onClose={()=>setOpen(false)} title="Command palette"><div className="flex items-center gap-2"><Search size={18}/><Input autoFocus placeholder="Cari izin, pasien, atau perintah..."/></div><div className="mt-3 space-y-1">{[['Buka ringkasan','/desktop'],['Buka antrian izin','/desktop/queue'],['Buka audit trail','/desktop/audit'],['Pengaturan demo','/desktop/settings']].map(([l,p])=><button key={p} onClick={()=>go(p)} className="w-full rounded-xl px-3 py-2 text-left text-sm hover:bg-slate-100 dark:hover:bg-slate-800">{l}</button>)}</div></Modal>}
